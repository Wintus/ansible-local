---

- name: install Go
  when: ansible_pkg_mgr == "apt"
  become: yes
  apt:
    name: golang

- name: set $GOPATH in profile
  template:
    src: golang.sh
    dest: "{{ bash_profile_dir }}/"
    mode: a+x

- import_tasks: hub-compiled.yml
- import_tasks: hub.yml
- import_tasks: ghq.yml
