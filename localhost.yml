---
- hosts: localhost
  connection: local
  become: yes

  roles:
    - role: ubuntu
      when: ansible_distribution == "Ubuntu"

    - role: ubuntu/ansible

    - role: ubuntu/gnome

  tasks:
    - name: install git
      apt:
        name: git
      when: ansible_distribution == "Ubuntu"
